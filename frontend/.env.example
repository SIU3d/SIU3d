# WebSocket endpoint the client should use (frontend falls back to ws://<host>:4000)
VITE_BACKEND_WS=ws://localhost:4000

# Optional: proxy target used by Vite dev server for /health (helps when backend runs remotely)
VITE_BACKEND_PROXY=http://localhost:4000
